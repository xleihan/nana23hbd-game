/**
 * Configuration file for the Lyria Rhythm game.
 * Groups all configurable items (like model names) in a centralized file.
 */

export const CONFIG = {
  models: {
    // Used for full-length tracks (Lyria Pro)
    fullTrack: 'lyria-3-pro-preview',
    // Used for shorter, 30-second clips (Lyria Clip)
    quickClip: 'lyria-3-clip-preview'
  }
};
